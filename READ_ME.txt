main.py parses anon.json and produces network_graph.html.
Play around with weights and thresholds in main.py to get different graphs.
min_weight_ratio is the variable deciding how selective the algorithm is. A good range is [0.6 0.8].